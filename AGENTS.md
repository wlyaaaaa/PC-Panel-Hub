# PC Panel Hub 项目规则

- 机箱 TURZX 屏负责密集电脑指标；LIAN LI HS2 曲面屏负责时钟、媒体、任务和告警。不要把两块屏的职责或健康结论混为一谈。
- 写 TURZX 实体串口前，确认唯一健康的目标设备和独占串口；`COM7` 只是本机默认。已验证的全帧发送与设备特定的混合刷新要分清，后者没有实体像素确认。
- 保持现有每秒混合刷新与有界恢复；只在明确诊断兼容性时使用三秒全帧方式，不以降低正常刷新频率掩盖冻结。
- HS2 恢复保留本次启动已成功枚举的模式。只有唯一健康的控制器、显示设备及 LED 绑定持续满足条件，才能启动完整浮层；普通窗口保护可在目标屏和窗口唯一识别后先行。
- 常规看门狗不得重启 USB Hub、删除设备或扫描 PnP。HS2 硬件恢复只走 `scripts/repair-hs2.ps1` 的显式 `-Apply`，并核对专用 Hub、端口和同属设备；身份不明就停。
- 睡眠恢复只由现有常驻看门狗负责，旧 Resume 任务保持禁用。TURZX 自动恢复只触及已确认的单一串口端点。
- VDD 与 HS2 的普通窗口保护共用排除边界：Bubbles、Emerald Veil、实体黑屏遮罩、浮层、Wallpaper Engine 和桌面外壳不得搬移或最小化；无唯一目标时零动作。
- 不向公开仓库提交厂商二进制、本机配置、设备拓扑、日志、凭据或私人截图。引用厂商行为时标明观察边界。
- TimeAudit 的帧数据只走只读接口，不改其原项目；在本人明确认定副屏最终完成前，不上传 Google Drive。
- 常规回归见 `scripts/test.ps1`，优先运行无硬件写入的测试。构建、心跳、进程或串口写入成功均不能代替实体刷新、睡眠恢复和断电重启验收。
- 后台启动必须真正无窗口；安装任务、部署、显示切换、串口实测和 USB 操作须按当前任务的明确范围执行，结果与源码测试分开记录.
